# Build output
bin/

# Database dumps produced by `task db:backup` (see BACKUP.md) — never committed
backups/

# Local-only env files (generated by `task dev`)
.env
.env.local
.env.prod
mcp

# flutter
apps/companion/devtools_options.yaml

.mcp.json

corelyr-kazper-*.json
google-services.json
# Built SPA bundle — produced by `task web:build` / the Docker node stage, not committed
# (per build-spa-in-pipeline). The binary embeds it under `-tags webembed`.
apps/web/dist/
# MkDocs build output (userdocs/ is the source)
site/
