## Continuity

_Forward plan for OpenSpec changes. Tracks **what's next**, **what's in flight**, and **what's queued**._
_This file is the operational queue; it carries its own triage framing in Up next + Backlog._
_Last refreshed: 2026-07-15 by the `continuity` skill (**`add-multisport-segment-durations` applied AND archived** — multisport template reads (single+list) now carry a derived `estimated_duration_sec` on each **sport** segment (sum of its time-bound step durations, null if not fully time-bounded — same rule as the template-level total; transitions keep their explicit `duration`). Serialization-boundary in `stampDuration`, re-derived per read, never persisted; **response-only → golden unchanged**. No migration. Commits `92a64da` feat + `68993c9` archive. Multisport package green; `swag`/`vet`/`validate --strict` clean. **Up next now leads with `add-meal-product-correction`.** `main` is ahead of `origin/main`)._

## In progress

| Change | Branch | Started | Owner | Notes |
|---|---|---|---|---|
| _(none)_ | | | | |

_Nothing in flight. `extend-plan-adherence-detail` archived 2026-07-08._

## Up next

Ordered queue — top is next to pick up. _(**To take the public race site live, do the k8s cutover operator step — see the `host-public-site-in-cluster` note.**)_

1. **add-meal-product-correction** — `Why:` Oldest triage item: `POST /meals/{id}/correct-product` re-derives nutrients from a product (same path as product-mode create) while preserving id/`logged_at`/note/workout link; works freeform→product and product→product; `correct_meal_product` write MCP tool. No migration. _Proposed 2026-07-14; valid, uncommitted._
2. **add-supplement-log** — `Why:` Triage item: new `supplements` capability — timestamped events (`name` required, paired `dose`/`dose_unit`), POST/window-GET/GET/DELETE (no PATCH — delete+relog), `log_supplement`/`list_supplements`, today's entries folded into `/context/daily`; export-included. **Carries a migration (head now 062 → 063).** _Proposed 2026-07-14; valid, uncommitted._
3. **add-sweat-rate** — `Why:` Triage item T2 #6C: `GET /workouts/{id}/sweat-rate` field test — explicit `pre/post_weight_kg` params (daily bodyweight grain can't fake pre/post), fluid itemized from linked hydration + workout-fuel ml (+ override), implausible results warn not refuse; `sweat_rate` MCP tool; lands in `internal/workoutfueling/`, delta in the `workout-fuel` spec. No migration. _Proposed 2026-07-14; valid, uncommitted._
4. **rename-nutrition-chat-spec** — `Why:` Docs-only debt: `openspec/specs/nutrition-chat/` → `coach-chat` + a real Purpose (TBD since 2026-06-14). Capability renames aren't delta-expressible — seeded delta + hand `git mv` at archive (the coach-memory precedent). Zero code. _Proposed 2026-07-14; valid, uncommitted._

## Backlog

_Unproposed candidate seams (ideas, no artifacts yet):_

- **GoldenCheetah/Intervals.icu gap analyses (2026-07-14) — fully triaged.** Shipped: `add-critical-power-model`, `w-prime-balance`, `add-wellness-diary`, `power-profile-percentile-ranking`. Proposed (Up next #2–4): `add-interval-detection`, `add-durability-analysis`, `add-cadence-quadrant-analysis`. _Deliberately skipped: Aerolab CdA, Banister model, data-fix tools, maps (standing non-goal)._
- **2026-07-14 backlog sweep — everything proposal-shaped is now proposed (Up next #5–13):** the Intervals.icu residuals (eFTP history → `add-cp-model-history`, per-sport split → `add-per-sport-pmc`, wellness correlation → `add-wellness-tsb-correlation`), the plan-adherence open questions (→ `polish-plan-adherence-detail`), multisport Phase 4 (→ `add-multisport-segment-durations`), and the old triage trio (→ `add-meal-product-correction`, `add-supplement-log`, `add-sweat-rate`), plus the `nutrition-chat`→`coach-chat` rename (→ `rename-nutrition-chat-spec`).
- **GPS tracks / map view** — deliberately skipped in the TrainingPeaks gap analysis (Garmin's own app covers maps; low coaching value for this backend). Revisit only if course-specific race pacing ever becomes a goal.

## Notes

- **`host-public-site-in-cluster` — ARCHIVED 2026-07-14** (synced 2 MODIFIED `public-race-site` reqs + 1 ADDED `deployment-pipeline` req; commits `67113bd` feat + `9cf002f` delta-fix + `6cf136e` archive). **Re-decides `add-public-race-site`'s D1**: serve the public "road to race" page from the **k8s cluster** (the athlete already runs it) instead of GitHub Pages — one deploy surface. Same baked static `dist/`, new serve layer. **Consciously re-accepts** the one property the static-on-Pages design avoided (public traffic on the cluster ingress) because the exposure is narrow — a **stock `nginx` on its own ingress host with NO route to the API** — and the **feed secret never enters the cluster** (build-time bake). New `apps/public/Dockerfile`: multi-stage — `node:22` build runs the existing `npm run build` with `FEED_SECRET` as a **BuildKit `--mount=type=secret`** (out of every layer + build history) + `FEED_URL`/`SITE_URL` build args → **`nginxinc/nginx-unprivileged:alpine`** (non-root uid 101, port 8080) serving the baked files; `.dockerignore`. The `public-site.yml` workflow **retargeted Pages→GHCR**: `docker/build-push-action` (feed secret as a BuildKit `secrets:` entry, FEED_URL/SITE_URL build args) pushes `:latest`+`:sha-…` then a **kubeconfig-gated `rollout restart`** rolls the Deployment; **a failed build never pushes or rolls** (previous image serves; countdown recomputes client-side so it degrades to stale, never broken); Pages perms/environment dropped. Helm: opt-in **`publicSite` block** (default off → renders nothing, the `backup.enabled` precedent) — `templates/public-site.yaml` gated Deployment (nginx image, `runAsNonRoot`/uid 101, port 8080, **no `FEED_SECRET` env/mount**) + Service + a **dedicated public Ingress** (cert-manager TLS) whose **only backend is the public-site Service** (never the API); new `public-site.*` `_helpers.tpl`. Docs: chart README `publicSite` section + `apps/public`/top README topology moved to in-cluster. **`helm lint` caught a real bug** — a `{{- … -}}` assignment right after `---` collapsed into `---apiVersion` (moved the `$tlsSecret` var to the top block). **OpenSpec archive guard caught another** — the MODIFIED secret-hygiene requirement had a *renamed* scenario header (reads as dropping a scenario); restored the original header with extended image-layer content, then archived. Verified: **docker build against a mock feed** → serves index.html (race + countdown) + og.png, runs as uid 101, **0 secret + 0 FEED_URL leaks** in image history *and* filesystem; **`helm template`** off→0 objects / on→3 objects (ingress backend = public Service only, no `FEED_SECRET`, `runAsNonRoot`); **`helm lint` clean**; `go build` unaffected (no Go touched). **Applied + archived this session (arrived via `/opsx:update`→propose earlier today).** **⚠️ Operator cutover pending (5.4, unchecked — not runnable here):** enable `publicSite.enabled`+`host`+image tag, add `FEED_SECRET`/`FEED_URL` to the CI/build env, **point public DNS at the cluster ingress**, verify the live page + TLS + OG card, then retire any Pages remnant. _(This supersedes `add-public-race-site`'s four Pages/Actions operator follow-ups — those are now moot; the k8s cutover is the single remaining operator task.)_ _Non-goals: GitOps auto-image-bump specifics, rate-limit/WAF on the public ingress, custom domain beyond the ingress host._
- **`add-multisport-segment-durations` — ARCHIVED 2026-07-14** (synced 1 ADDED `multisport-workouts` req; commits `92a64da` feat + `68993c9` archive). The carried "multisport Phase 4 nicety": Phase 3 gave the *template-level* `estimated_duration_sec`; this adds the **per-sport-segment** breakdown ("swim ~35 min, bike ~2:10, run ~55") so the client needn't re-derive from steps. `segmentEstimatedDurationSec` sums a sport segment's time-bound step durations (**null when not fully time-bounded** — the same rule as the total); **transitions carry their explicit `duration`, no estimate**. Set in `stampDuration` (indexing the slice) alongside the template total, **re-derived on every read, never persisted** — so a write can't corrupt it. **Response-only field → no MCP input-schema change, golden unchanged** (confirmed via goldengen: empty diff). No migration, no new endpoints/tools (rides the existing template shapes over REST + verbatim MCP). Verified: DB tests (fully-bounded tri per-segment values + total reconciliation, one-unbounded-segment null isolated to that segment while a sibling keeps its estimate); multisport package green; `swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived.** _Deferred (non-goals): distance-based duration estimation (pace-dependent), per-segment durations on materialized planned workouts (template view only)._
- **`polish-plan-adherence-detail` — ARCHIVED 2026-07-14** (synced 2 ADDED `workouts` reqs; commits `8ce6dee` feat + `e52dfaa` archive). Closed the two `extend-plan-adherence-detail` open questions. **`missed_limit`** ([1,200], default 50, `missed_limit_invalid`): **truncation moved OUT of `computeAdherence`** (now builds the full sorted missed list) **into `applyMissedLimit` at the presentation boundary** — so a raised limit isn't clipped at 50 first; `missed_sessions_truncated` semantics preserved. **`zero_fill=true`** (opt-in): `zeroFillWeekly` materializes every empty week between the first and last present bucket — **calendar Mondays** (7-day step) or **plan-week ordinals** (week_start extrapolated at 7 days/ordinal from a present anchor) — zeroed counts, null rate + null durations; a <2-bucket trend is unchanged. **Omitting both = today's response byte-for-byte** (the `computeAdherence` fold untouched). `workout_adherence` MCP forwards both optional args (golden input-schema touch). **No dashboard** (no adherence panel yet — zero-fill enables one later). No migration. Verified: internal units (missed-limit boundary incl. raised-limit-keeps-tail, zero-fill calendar + plan-ordinal continuity, <2-bucket no-op) + endpoint (missed_limit 400 matrix, zero_fill+raised-limit contiguous axis) + registry build + MCP golden/integration; **full Go suite green** (racepacing/workoutfuel flake — isolated pass). `swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived.** _Deferred: raising the default, an adherence dashboard panel, per-sport trend splits._
- **`add-wellness-tsb-correlation` — ARCHIVED 2026-07-14** (synced 2 ADDED `wellness-diary` reqs; commits `ec27e09` feat + `f4b509c` archive). The diary's deferred second half — subjective scores held against objective load. `GET /wellness/correlation?from&to&tz&metric` pairs each wellness field's daily entries with the **same-day** PMC value (`metric`: `tsb` default | `ctl` | `ramp_rate`) and returns the per-field **Spearman rank correlation** `{n, rho}`; a field with **< 14 paired days → `{n, reason:"insufficient_pairs"}`** and no rho (early sparse data can't produce a confident number — visible progress, not absence). **Pure tie-aware Spearman** (average-rank → Pearson-on-ranks; unit-tested: perfect ±1, matching-ties, zero-variance→0, rank-averaging); pairing is same-day by the athlete's calendar date, **no interpolation/carry-forward**. `internal/wellness` consumes a narrow **`PMCProvider` interface** (`PMCValues(from,to,loc)`) **cross-injected in `Run()`** via a `pmcWellnessAdapter` over the pmc service — no pmc import, no cycle, no duplicate EWMA (the FK-cross-injection precedent). **Association only** (the tool description says so — confounders abound, rho on 5-level data saturates). Metric `tsb`/`ctl`/`ramp_rate` (`metric_invalid`); 92-day range cap. `numfmt.Round2` on rho. MCP `wellness_correlation` (read; association-not-causation framing; golden additive). **No dashboard v1** (a coach-conversation read). Verified: Spearman units + endpoint integration (16-day negative-association fixture rho<−0.8, 5-entry sparse gate, metric matrix incl. default, range 400s) via a **fake PMCProvider** + registry build + MCP golden/integration; **full Go suite green (59 pkg, first run)**. `swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived.** _Deferred: lagged/causal (rho at lag k), Garmin-vitals (sleep/HRV) correlation v2, dashboard charting._
- **`add-cp-model-history` — ARCHIVED 2026-07-14** (synced 2 ADDED `effort-analytics` + 1 ADDED `coach-dashboard`; commits `b5e23be` feat + `c324517` archive). The eFTP-trend v2: `GET /workouts/cp-model/history?from&to&tz&window_days` fits the CP2 model at each **Monday anchor** in the range, each over its **trailing `window_days`** (default 90, bounds [30,365], `window_days_invalid`), **reusing `CPModelFor` per anchor** (one windowed-MAX fit each). Per anchor: the fitted model or **null + the gate `reason`** — the null anchor is kept so the trend **gaps, not zeroes**. Shared power-curve range/tz contract (400-day cap); `numfmt` per-anchor at the boundary; **advisory** (never reads athlete-config — cp-model D4 holds). Compute-on-read, no migration. MCP `cp_model_history` (read; description points at `athlete_config_history_get` as the configured-FTP counterpart; golden additive). `/stats` CP panel gains a **CP-over-time trend** (visx, gapped at null anchors via segment-splitting) with the **configured-FTP step line overlaid client-side** (new `useThresholdHistory` hook over `/athlete-config/history` — the backend stays uncoupled). Verified: endpoint integration (season series + Monday alignment, all-gapped-anchors-retain-reason, `window_days` matrix, range 400s, read-only) + registry build + MCP golden/integration; **80 web tests** (trend + FTP overlay present/absent) + `tsc`; **full suite green modulo the boot-contention flake** (isolated re-runs pass). `swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived.** _Deferred: persisting fit history, per-anchor W′ trend charting (returned, not charted), auto-apply thresholds._
- **`add-per-sport-pmc` — ARCHIVED 2026-07-14** (synced 1 ADDED `performance-management` + 1 ADDED `coach-dashboard`; commits `6f2cf64` feat + `669f94f` archive). Optional `sport=` on `GET /performance/pmc` — the CTL/ATL/TSB EWMA over only that sport's completed-workout TSS (warm-up/seed_date/ramp-alerts/missing-TSS all within the filtered series). Repo `($N IS NULL OR sport=$N)` predicate on both queries (earliest-date + daily-SUM); **omitted → byte-identical combined** (no `sport` key), `sport_invalid` on an unknown value (validated via `workouts.ValidSport`), response echoes `sport` when filtered. **Multisport counts as its own sport** (no per-segment TSS split — the workout-stats `by_sport` precedent). Service EWMA unchanged (same math over a filtered series). No migration. `pmc_series` MCP gains the optional `sport` arg (golden input-schema touch; description notes combined ≠ sum-of-per-sport). `/stats` PMC panel gains an All/Bike/Run/Swim selector; the **target-CTL overlay shows only on All** (the trajectory is combined-CTL). Verified: run-filtered vs combined (180 vs 60 TSS same day, per-sport seed, lower filtered CTL) + omitted-byte-identical + multisport-as-own-sport + `sport_invalid` + registry build + MCP golden/integration; **78 web tests** (disambiguated the power-curve sport buttons — PMC now adds a second Bike/Run/Swim toggle) + `tsc`; **full Go suite green** (bodyweight/garminauth/recoverymetrics were the boot-contention flake — pass isolated). `swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived.** _Deferred: per-segment multisport TSS attribution, per-sport target trajectories (composes with planned-vs-actual-ctl), side-by-side multi-sport in one response._
- **`add-cadence-quadrant-analysis` — ARCHIVED 2026-07-15** (synced 2 MODIFIED + 2 ADDED `activity-streams` reqs + 1 ADDED `garmin-bridge` + 1 ADDED `coach-dashboard`; commits `64ae49d` feat + `bcbeb2a` archive). The last schema-touching GC gap, sequenced last. **Migration 062** widens the `workout_streams.stream_type` CHECK (056) to accept **`'cadence'`** (rpm) — another 1 Hz REAL[] series, stored/retrieved/downsampled/replace-on-repost exactly like heart_rate, but feeding **no** best-effort, execution metric, or energy total (unit-isolation extends; `Repo.Replace`'s hardcoded type list gained `StreamCadence`, ingest/retrieval are otherwise generic). The **garmin-bridge** `_extract_streams` pulls **`directBikeCadence`** defensively (gap→0, all-non-positive dropped, out-of-range descriptor → no array → sync unaffected; the `directPower` precedent) into the same POST — **deploy backend before bridge** (new bridge + old backend gets its cadence array CHECK-rejected). `GET /workouts/{id}/quadrant?cp_watts=&cadence_rpm=&crank_mm=` runs pure `quadrant.go`: per sample `CPV = cadence·crank·2π/60` (m/s), `AEPF = power/CPV` (N), classified against the reference point (`aepf_ref = cp_watts/cpv_ref`, `cpv_ref` from `cadence_rpm`; **crank 172.5 mm default**, valid [100,220]) into 4 Coggan quadrants (I high-force/high-velocity … IV low-force/high-velocity); **samples with power≤0 or cadence≤0 excluded and counted** (`excluded_s` — coasting isn't pedaling, mustn't dilute shares). Returns shares + `pedaling_s`/`excluded_s` + refs + a **≤1000-point systematically-downsampled scatter** (`summary_only` omits it). **Explicit params, W′bal convention** (`cp_invalid`/`cadence_invalid`/`crank_invalid`); sentinels `workout_not_found`/`streams_not_found`/`power_stream_missing`/**`cadence_stream_missing`** (history has no cadence until a post-bridge re-sync); shares `Round1`, refs/scatter `Round2`; compute-on-read. MCP **`quadrant_analysis`** (read, **`summary_only=true` hardcoded** — scatter is chart data, D5). `/workouts/:id` gains a **quadrant scatter** (visx, reference cross-hairs + shares legend) beside the W′bal strip, gated on power+cadence + a CP fit, **90 rpm** UI pivot; absent otherwise. Verified: pure quadrant units (reference-point math, grinding→Q2, spinning→Q4, coasting-excluded, crank sensitivity, scatter cap, all-coasting-empty) + endpoint integration (happy Q2, summary_only, crank param, 400 matrix, **4-sentinel matrix incl. cadence_stream_missing**, read-only) + cadence stream-plumbing integration (stored-but-no-derivation) + registry build test + MCP golden/integration + **bridge pytest (4 new cadence cases; 157 total)**; **77 web tests** + `tsc` (scatter present/absent); **full Go suite green (59 pkg, first run)**. `task swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived.** _Deferred (non-goals): run cadence (spm) analysis, torque-effectiveness/pedal-smoothness (Garmin-computed), crank in athlete-config, `6.2`/`6.3` operator deploy-sequencing + live post-deploy verify (manual — confirm the `directBikeCadence` key on the first real sync)._
- **`add-durability-analysis` — ARCHIVED 2026-07-14** (synced 1 MODIFIED + 2 ADDED `effort-analytics` reqs + 1 ADDED `coach-dashboard` req; commits `2811ec4` feat + `cfc8589` archive). The modern long-course metric both gap analyses flagged: not what power the athlete produces, but **what power survives deep into a ride**. The best-efforts ladder was fatigue-blind — a fresh 20-min best and one 2000 kJ in counted the same. **First migration of the day (`061`)**: `workout_best_efforts` gains **`kj_tier SMALLINT NOT NULL DEFAULT 0`** and the unique key widens to `(workout_id, metric, duration_s, kj_tier)` — **tier 0 IS the existing fresh ladder** (D1: one table, not a parallel `durability_efforts`; the default back-fills existing rows atomically). Stream **ingest + recompute** (inside the existing `ComputeAndReplace`, one extra pass — cumulative kJ is a running prefix sum) additionally derive, **for power only**, the mean-maximal at **1m/5m/20m** after **500/1000/1500/2000 kJ** — the best window whose **first sample lies at/after** the tier's accumulated work (a strict "produced while already that deep" reading, `sort.Search` on the monotone prefix); **rows only for tiers the ride reaches** *and* only when a full window fits after the tier (a 720 kJ hour reaches tier 500 but its 20-min window doesn't fit → 1m/5m only). **The critical safety property**: every existing consumer (power-curve / cp-model / power-profile all read through `Repo.Curve`) now **pins `be.kj_tier = 0`**, so tiered rows can never leak into the fresh curve — regression-tested, and structurally safe anyway (a tier window ⊆ the ride, so tier ≤ fresh, MAX unaffected). `GET /workouts/durability?from&to&tz` returns per durability-duration the `fresh` best + each tier `{kj_tier, watts, fade_pct=(fresh−tier)/fresh×100, workout_id, date}`; **tiers absent in the window omitted**, a **fresh-only window → `200 reason:"no_tiered_data"`** (historical rides gain tiers only after **recompute** re-derives their stored streams — the `persist-activity-streams` backfill path, D3). Power-curve range/tz contract; **compute-on-read over stored rows, no stream scans** (D4 — the alternative, scanning every stream per request, would be hundreds of full-stream reads). MCP **`durability`** (read, verbatim; description flags the recompute-backfill dependency so a `no_tiered_data` on old data reads as "backfill not run", not "no efforts"). `/stats` **durability fade grid** (durations × tiers, fade-colored, own window selector defaulting 365d, **empty state pointing at recompute**). Verified: pure tier-derivation units (1600 kJ reaches 500/1000/1500 not 2000, window-fit per tier, short-ride-no-tiers, declining-power fade, empty) + endpoint integration (fade happy path, **tier-leak regression on the fresh curve**, no-tiered-data, empty window, re-post-replaces-tiers, 400 matrix, read-only) + registry build test + MCP golden/integration; **updated two existing count assertions** (3600 s rides now write +2/+3 tiered rows — documented) + `service_internal` `meanMaximal` unaffected; **75 web tests** + `tsc` (grid + empty state; **CP-window test re-pinned to absolute toggle index 1** — durability is the 4th `90/180/365` panel now); **full Go suite green** (`bodyweight` was the boot-contention flake — passed isolated after a prune). `task swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived this session.** _Deferred (design non-goals): run/swim durability, a same-ride mode, a durability *score* (the table is the product), configurable tiers/durations, a 2500 kJ tier, `6.2` live backfill (manual — recompute long historical rides)._
- **`add-interval-detection` — ARCHIVED 2026-07-14** (synced 2 ADDED `activity-streams` reqs + 1 ADDED `coach-dashboard` req; commits `4ef0741` feat + `eb40f44` archive). Double-voted (GoldenCheetah's effort finder + Intervals.icu's namesake): Kazper could score *planned* structure (step-compliance) but was blind inside *unstructured* rides. **No migration, lands in `internal/activitystreams/`** (the per-workout stream-compute home, W′bal's neighbor). `GET /workouts/{id}/intervals` runs a **deterministic, parameter-free** pipeline in pure `intervals.go`: **30 s centered rolling mean** → a work/rest **threshold from the ride's own smoothed power distribution** (no CP, no config — structure not physiology, D1: tempo intervals in a recovery ride are plainly intervals) → **gap-merge ≤30 s** (a gear lull doesn't split an effort) → **discard <60 s** (sprints are best-effort territory). Returns `threshold_w`, each interval's `{n,start_s,end_s,duration_s,avg_w,max_w,kj}` **computed over the RAW stream** (the smoothed series only sets boundaries), the rest gaps, and a summary (count, work_total_s, mean_effort_s/w). **Bimodality gate — the one real subtlety:** the design specified Otsu between-class variance (η) as the "meaningfully bimodal" test, but **η runs ≈0.64 even for Gaussian noise** (any spread splits high), so a naive η floor manufactured 10 "intervals" from a sine. Switched the gate to a **histogram-valley test** — the sample density at the Otsu split must be well below the two surrounding modes' density; a steady ride's split sits on its single dense peak (rejected), an interval ride's sits in a sparse valley (accepted). Steady ride → **`200 intervals:[] threshold_w:null reason:"no_distinct_efforts"`** (absence is a real coaching answer, not an error, D3). Sentinels mirror the siblings: `workout_not_found`/`streams_not_found`/`power_stream_missing`; watts rounded whole, kJ `Round1` at the boundary. MCP **`detect_intervals`** (read, one GET, **full body verbatim** — unlike raw series, a detected-interval list is compact structured reasoning data, D5). `/workouts/:id` gains a **Detected-intervals table** beside splits (n/duration/avg-W/max-W/kJ/rest-after + threshold + summary), **rendered only when ≥1 interval** — `no_distinct_efforts`, no power, or a fetch error render nothing (D6). Verified: pure unit tests (5×4-min exact count + boundaries, dip-doesn't-split gap-merge, sub-60 s burst discard, steady-ride null via a **deterministic LCG noise fixture** — a sine is intrinsically bimodal/arcsine and was a bad fixture, trimodal lumping documented) + endpoint integration (structured ride, steady-ride null, sentinel matrix, no-mutation) + registry build test + MCP golden/integration; **73 web tests** + `tsc` (routing + wprime mocks extended, table present/absent); **full Go suite green (59 pkg, first run)**. `task swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived this session.** _Deferred (design non-goals): run/swim pace detection, planned-vs-detected comparison (step-compliance owns it), persistence, auto-labeling, tunable constants, painting boundaries on a per-workout power chart, `5.2` live sanity (manual — a real interval session + a steady ride)._
- **`add-planned-vs-actual-ctl` — ARCHIVED 2026-07-14** (synced 2 ADDED `performance-management` reqs + 1 ADDED `coach-dashboard` req; commits `258b1c6` feat + `95b15b4` archive). The carried PMC-v1 deferral, now that PMC + macrocycle phase targets + honest per-sport TSS coexist: hold the declared ramp against the measured curve. **No migration, lands in `internal/pmc/`** (the compute-on-read posture). `GET /performance/pmc/target-trajectory?macrocycle_id=&tz=` simulates the **target CTL curve** — daily target TSS = the containing phase's `target_weekly_tss/7`, folded through the **same 42-day EWMA** as the measured PMC, **seeded from the actual CTL on the macrocycle start date** (D2 — day-0 delta is zero, we ramp from where the athlete actually is) — beside `actual_ctl`/`delta` for days up to today (future days carry the target alone). Each day: `target_ctl` + `target_declared` (**false for gaps / null-target phases, which decay** — an undeclared block *means* decay for the declared plan, D4). Summary: `current_delta` (>0 = ahead), `delta_trend_14d` (simple now-minus-14-days-ago difference), and **two end projections** — `projected_end_ctl_planned` (pure plan) vs `projected_end_ctl_current` (EWMA extended from today's actual over the remaining planned targets: "if you follow the plan from here"). **Resolution** (D3): `macrocycle_id` optional → the **active macrocycle** (containing today, **latest start_date tie-break — the public-race-feed rule**, NOT `CoveringFor`'s updated_at); unknown id / none active → **`404 macrocycle_not_found`**; a macrocycle whose phases declare **no** target → **`200` `trajectory:null` + `reason:"targets_missing"`** (the CP-model null posture — an all-zero season is confidently meaningless). **Pure `simulate` extracted** (no DB — unit-tested: equilibrium invariant, seeded-start, gap decay+flags, actual/delta-up-to-today, under-training projection divergence); the actual curve + seed reuse `SeriesFor` verbatim (mathematically comparable). Narrow **`MacroResolver`** interface injected in `Run()` (adapter over `macrocycle.Repo.List`/`GetByID`, keeps pmc cycle-free). `numfmt.Round1` at boundary, missing-TSS inherited from the PMC. MCP **`pmc_target_trajectory`** (read, one GET; description distinguishes it from `pmc_series`'s measured-only + names the active default; golden additive). `/stats` PMC panel gains a **dashed amber target overlay** (only the window-overlap drawn; **undeclared days muted** as faint dots) + a **`TargetReadout`** ("on plan / −N behind" + 14-day trend + end-CTL current-vs-planned) — **absent on 404 / `targets_missing` / fetch error**, the measured panel unchanged. Verified: pure sim units + endpoint integration (active happy path, explicit id, gap-flagged spans, targets-missing null, no-active/unknown 404, future-days-target-only, bad-tz, nothing-persisted) + registry build tests + MCP golden/integration; **71 web tests** + `tsc` (overlay renders + absent states + readout values; PMC-window test unaffected); **full Go suite green** (`mealplan` was the testcontainers boot-contention flake — passed isolated after a container prune). `task swag`/`vet`/`validate --strict` clean. **Proposed (parallel session) → applied → archived this session.** _Deferred (design non-goals/open Qs): hours-based targets, ATL/TSB target sim, catch-up/re-planning suggestions, per-sport target trajectories (compose once `add-per-sport-pmc` lands), `5.2` live sanity (manual — check `seed_ctl` vs the PMC series at start date)._
- **`power-profile-percentile-ranking` — ARCHIVED 2026-07-14** (synced 4 ADDED `effort-analytics` reqs + 1 ADDED `coach-dashboard` req; commits `30a3401` feat + `f4ea272` archive). The cheapest remaining GoldenCheetah gap: turn the existing power curve into a "how good is this" answer. **No migration, no new package** — extends `internal/effortanalytics/` (the CP-model posture). `GET /workouts/power-profile?from&to&tz&weight_kg&sex` **reuses `CurveFor`** (power) to pull the windowed best at **four Coggan anchors** — 5 s (neuromuscular), 60 s (anaerobic), 300 s (VO₂max), **1200 s as a 20-min FT proxy** (no 0.95 haircut — transparency over a hidden fudge) — divides by weight, and ranks each W/kg against the **embedded Coggan tables** (eight named categories per sex, from Allen & Coggan; user chose "standard published values" accepting middle-row decimal risk). Per anchor: `watts`, `w_per_kg`, the **`category` band (authoritative)**, an **interpolated `percentile` (0–100, an estimate** — linear between adjacent rows, clamped; D2), workout/date. **`phenotype`** (sprinter/time_trialist/climber/all_rounder) from the relative sprint-vs-endurance percentile spread, **null unless all four anchors present** (D5). Missing anchors **named, not fabricated** (per-duration empty-window posture). **Weight resolution** (energy-endpoint precedent, D3): `weight_kg` param (>0, else `weight_kg_invalid`) → latest stored bodyweight via a narrow **`WeightProvider` interface** (adapter over `bodyweight.Repo.LatestBefore` in server.go — effort-analytics stays decoupled) → else `400 weight_data_missing`; `weight_source` echoed. **`sex`** selects the table (`male` default, `sex_invalid` otherwise; athlete-config has no sex field — the param is the seam, D4). **Advisory** — never reads/writes athlete-config; shared `parseWindow` contract (400-day cap); `numfmt` at boundary (W/kg + percentile 1dp). MCP `power_profile` (**read**, one GET, `weight_kg`/`sex` forwarded only when set — the endpoint applies its own fallback/default; golden regen additive, only that tool). `/stats` **Coggan panel**: per-anchor W/kg + category badge + percentile bar + phenotype label + weight provenance, own 90/180/365 selector, **degrades on `weight_data_missing`** ("add a body-weight entry") — not an error banner. Verified: pure ranking unit tests (published-anchor cells per sex, interpolation monotonic + clamped, every phenotype branch + nil-on-incomplete, FT no-haircut) + endpoint integration (four-anchor rank, missing-anchor omit+list, weight param/stored/missing, sex/weight 400s, window matrix, no-mutation, phenotype present/null) + registry build tests + MCP golden/integration; **68 web tests** + `tsc` (fixed the CP-window test's 365d button index — third `90/180/365` toggle now on the page); **full Go suite green** (`bodyweight`/`trainingphases` were the testcontainers boot-contention flake — pass isolated `-p 1`; pruned mid-session). `task swag`/`vet`/`validate --strict` clean; README MCP row. **Proposed→applied→archived this session (explore→propose picked it from the GC backlog; user confirmed the standard-published-table sourcing).** _Deferred (design non-goals): run/swim critical-speed ranking, a configurable/custom reference table, a ranking-history trend, wiring the phenotype into `/context/training`, the public-site v2 `progress` number (this is its building block), `5.3` live sanity (manual — eyeball categories against known ability). A same-day duplicate proposal `add-power-profile-ranking` was withdrawn in favor of this one._
- **`add-wellness-diary` — ARCHIVED 2026-07-14** (synced NEW `wellness-diary` capability — 4 reqs — + 1 ADDED `daily-context` req; commits `819449b` feat + `73e4a12` archive). The Intervals.icu gap: Kazper ingested rich *objective* recovery (HRV/sleep/RHR/readiness/body-battery) but had no seam for *subjective* state. New `internal/wellness/` capability package (**migration 060**: `wellness_entries`, `entry_date DATE PK`, five nullable `SMALLINT CHECK BETWEEN 1 AND 5` + `note TEXT` + timestamps). **Per-date singleton, PUT full-replace upsert** (the `goals`-overrides pattern): `PUT /wellness/{date}` creates/replaces (absent fields cleared, not merged; **Idempotency-Key rejected centrally** by the PUT middleware), `GET`/`DELETE /wellness/{date}` (404 `not_found`), `GET /wellness?from&to` ascending (200 `{"entries":[]}` when empty — the athlete-config-history precedent — **92-day cap**, the nutrition-summary tier not the 400-day workout tier). Five OPTIONAL 1–5 scores keeping **natural direction** (fatigue/soreness/stress 1 none→5 severe; mood/motivation 1 low→5 high — not a forced uniform "higher is better"); **at least one field required** (`wellness_empty` rejects an all-null entry; a note-only entry is valid). Sentinels 1:1: `wellness_score_invalid`+`field` (out-of-range *and* non-integer — the JSON decode names the offending field via `UnmarshalTypeError`), `note_too_long` (2000-char service cap), `date_invalid`, `range_invalid`/`range_too_large`+`max_days`. Scores serialize `omitempty` — absent means not reported, never defaulted. **D5**: `/context/daily` folds in **today's entry only** as `wellness` (verbatim, **omitted entirely when absent** — never an empty object, never carried over from a prior day) beside the objective recovery snapshot, so subjective + device-measured read side by side; correlation stays in the coach's reasoning (no wellness×TSB SQL for v1). MCP `log_wellness` (**write**, wraps the PUT full-replace, no idempotency key — the description states score directions + **encourages partial entries over skipped days**) + `list_wellness` (read window); golden regen additive (only the two tools). `wellness_entries` classified **export-included** (user-authored, small, not re-derivable — the `athlete_config_history` classification; drift guard green). Verified: wellness package integration tests (partial-entry echo, full-replace-not-merge, note-only valid, empty-400, score-range matrix + non-integer naming the field, note cap, **Idempotency-Key-on-PUT 400** via mounted middleware, GET/DELETE 404s, window ascending + outside-range exclusion, empty-200, full 400 matrix) + daily-context tests (logged-day present with exact fields + rest-of-payload unaffected, unlogged-day omits the key with a JSON `NotContains` guard) + registry build tests + MCP golden/integration; **full Go suite green (59 pkg)**; `task swag`/`vet`/`validate --strict` clean; README MCP rows. **Applied + archived this session (arrived pre-proposed as the Intervals.icu find).** _Deferred (design non-goals): a dashboard wellness panel + companion-app entry UI (chat is the v1 input surface), wellness×TSB/PMC correlation analytics, structured injury tracking (freeform note carries it), Garmin subjective-import, carrying yesterday's entry into today's context, `5.3` live smoke (manual — needs the MCP client)._
- **`w-prime-balance` — ARCHIVED 2026-07-14** (synced 3 ADDED reqs — 2 `activity-streams` + 1 `coach-dashboard`; commits `f4b8580` feat + `9ec7cca` archive). The payoff feature the CP model parameterizes (the natural follow-up `add-critical-power-model` deferred): **per-workout W′ balance** — the anaerobic-battery drain/recharge story of a ride — over the already-stored 1 Hz power stream. **No migration, no new package** (lands in `internal/activitystreams/`, the stream owner). `GET /workouts/{id}/w-prime-balance` runs the **differential Froncioni–Clarke–Skiba recursion** in pure `wprimebal.go`: above CP `bal -= (P−CP)`, at/below CP `bal += (W′−bal)·(CP−P)/W′`, start `bal=W′`, **never clamped** (a negative floor / depletion > 100% is the honest "you went past your modeled W′" signal, not an error). **CP/W′ are explicit query params** (`cp_watts`/`w_prime_kj` from the `cp_model` fit) — this **settles cp-model's open question**: no auto-fit, no config columns, the endpoint never reads athlete-config (the advisory-only D4 posture carried forward). Returns a **summary** (`min_w_prime_kj`+`min_at_s`, `end_w_prime_kj`, `max_depletion_pct`, `time_below_25_pct_s`) + an optional **downsampled series** (kJ per point, reusing the existing bucket-mean helper, bounds `[10,5000]`); `summary_only=true` trims the series (the exact minimum stays in the summary regardless of downsampling). New sentinels `power_stream_missing` (HR-only workout → distinct from `streams_not_found`) / `cp_invalid` / `w_prime_invalid` (reject NaN/Inf/≤0); `numfmt.Round1` at the boundary. MCP `w_prime_balance` (**read tier**, one GET with **`summary_only=true` hardcoded** — the raw series stays chart-only, never a reasoning input; description points at `cp_model` as the parameter source + the "min<0 → re-fit" advisory); golden regen additive (only `w_prime_balance` added). Dashboard: `/workouts/:id` gains a **W′bal strip** (visx area + min marker + summary readout) that fetches the cp-model over a **trailing-90-day window** (the same fit the stats page shows) then the W′bal series — **renders nothing** when the fit is null OR the workout has no power stream (absence, not an error state — it's supplementary detail on this page). Verified: pure-model unit tests (constant supra-CP linear depletion hand-computed, depletion→recovery never exceeds W′, negative-floor >100%, all-sub-CP flat, time-below-25% accounting) + endpoint integration (happy summary+series, `summary_only`, downsample+bounds, param 400 matrix, `workout_not_found`/`streams_not_found`/`power_stream_missing` distinction via HR-only, no-mutation, unit isolation) + registry build test + MCP golden/integration; **65 web tests** + `tsc` (routing mock extended for the two new hooks + a dedicated `wprime.test.tsx` for the present/absent gating); **full Go suite green** (`athleteconfig`/`coachcontext` failures were the documented testcontainers boot-contention flake — pass isolated `-p 1`; leaked containers pruned mid-session). `task swag`/`vet`/`validate --strict` clean. **Applied + archived this session (arrived pre-proposed as the CP-model follow-up).** _Deferred (design non-goals): run/swim critical speed, auto-fit CP from the ride, a config-persisted CP/W′, `5.2` live sanity check (manual — needs a real hard interval ride)._
- **`add-critical-power-model` — ARCHIVED 2026-07-14** (synced 2 ADDED `effort-analytics` reqs + 1 ADDED `coach-dashboard` req; commits `f0e336e` feat + `f1214c9` archive). The GoldenCheetah gap analysis's top pick and cheapest to close: fit the **2-parameter critical-power model** over the already-stored mean-maximal best-efforts ladder → a **data-derived CP/W′ to sanity-check the configured FTP** against. **No migration, no new package** (lands in `internal/effortanalytics/`, the `add-performance-management` compute-on-read posture). `GET /workouts/cp-model?from=&to=&tz=` **reuses `CurveFor`** (the windowed per-duration MAX the power curve already serves), keeps the in-band points, and fits **OLS in work–time form** (`work_j = cp·t + w′`) in pure `cpmodel.go`: CP = slope (W), W′ = intercept (→ `w_prime_kj`); fit quality `r_squared` (work-space) + `rmse_w` (**power-space** residuals — "misses your 5-min best by 4 W"). **Validity band [120s,1800s]** → with the current ladder the in-band durations are 5m/10m/20m/30m (there's no 2m ladder point, so ≤4 points); gates `insufficient_points` (<3 distinct in-band durations) / `span_too_narrow` (longest <3× shortest), a gated response is still **200 with `model:null` + `reason` + whatever points were found** (the race-pacing degrade-with-a-reason posture). **Advisory only** — the endpoint never reads/writes athlete-config (D4); CP≈FTP interpretation + the compare-against-configured-FTP step stay with the consumer, which already has the FTP. Range/tz contract shared with power-curve (`range_required`/`date_invalid`/`range_invalid`/`range_too_large`+`max_days`/`tz_invalid`; ≥1yr) — factored a shared `parseWindow` helper. `numfmt` at the boundary (CP/RMSE/W′-kJ 1dp, R² 3dp). MCP `cp_model` (read, one GET verbatim; description carries the advisory CP≈FTP framing + points threshold *changes* at the athlete-config update flow); golden regen additive. `/stats` **Critical power panel**: CP/W′/R² readout + the effort points with the fitted hyperbola `P(t)=CP+W′/t` on log-x (visx, power-curve idiom), own 90/180/365 window selector, degraded state naming the gate reason. Verified: cpmodel unit tests (**exact-line recovery** CP 250/W′ 20kJ/R²=1/RMSE=0, noisy R²/RMSE, gate branches, band boundaries 120s-in/60s-3600s-out) + endpoint integration (fit-over-in-band, out-of-band exclusion, insufficient/empty, idempotent read, unit isolation, 400 matrix) + MCP golden/integration; **63 web tests** + `tsc` (fixed a pre-existing PMC test broken by the second 90/180/365 toggle); **full Go suite green (58 pkg)**; `task swag`/`vet` clean. `apps/web/dist` gitignored/pipeline-built (not committed). **Applied + archived this session (arrived pre-proposed via GC gap analysis).** _Deferred (design non-goals): W′bal per-workout depletion (the natural follow-up, consumes this CP/W′), run/swim critical speed, 3-parameter Morton, any auto-apply into athlete-config, `5.2` live sanity check (manual — needs real data)._
- **`add-public-race-site` — ARCHIVED 2026-07-14** (synced NEW `public-race-site` capability — 5 reqs; commits `aab199d` feat + `74c6005` archive). The deferred consumer half of `public-race-feed` (archived earlier today): a **static "road to race" broadcast page** friends visit, built from the secret-gated feed at CI build time. **First frontend/ops-only change** — no Go, no migration, no REST/MCP, no swag. **Supersedes `public-race-feed`'s Strapi-shield D1**: topology is now `GitHub Actions (cron) → X-Feed-Key → Kazper → Astro static build → GitHub Pages → friends` — at serve time there is **no server, no secret, and no path from public traffic to Kazper**; Kazper is touched once per rebuild by CI. New `apps/public/` (Astro, static output): `scripts/fetch-feed.mjs` reads `FEED_URL`+`FEED_SECRET`, GETs `/public/race-feed` **once** with `X-Feed-Key`, **fails the build on any non-200/network/missing-env** (Pages keeps the last deploy — degrades to *stale*, never *broken*), tolerates `{race:null}`, and writes only the `{race, days_remaining}` projection to a gitignored `src/generated/feed.json` (the secret + origin never enter shipped assets). The page (`index.astro`, read via `process.cwd()` **not** `import.meta.url` — Vite rewrites the latter at build, a bug caught in local verify where the OG card showed the race but the page showed off-season) renders hero countdown + name/date, or a **designed off-season face** on null; a **tiny inline countdown island** recomputes whole-days in the **viewer's local tz** floored at 0 from the embedded `race_date` (prerendered number is the no-JS fallback), so a stale build is only wrong about *which* race, never *how many days*. Build-time **OG card** (`gen-og.mjs`, `@resvg/resvg-js` SVG→PNG 1200×630, race + build-time countdown / neutral off-season variant) + `og:*`/`twitter:*` head meta. `.github/workflows/public-site.yml`: nightly cron (`15 23 * * *` ≈ after midnight Europe/Berlin) + `workflow_dispatch` + push filtered to `apps/public/**`, deploy to Pages; `dist` **gitignored** (CI-deployed, not `go:embed`-ed like `apps/web`). README topology updated to CI-as-shield + `apps/public` pointer. **Locally build-verified against a mock feed**: race render + hygiene (`grep` found 0 secret + 0 origin leaks, no runtime `fetch` in output), off-season null build, 503 + missing-env both exit non-zero, OG PNG 1200×630 in both variants. **⚠️ FOUR OPERATOR FOLLOW-UPS pending (left unchecked in tasks.md — not executable from this environment):** (4.2) enable **GitHub Pages** for the repo + add `FEED_SECRET` **Actions secret** and `FEED_URL` **Actions variable**; (4.3) confirm a bad-secret run fails and leaves the previous deploy serving; (5.2) end-to-end verify against the **live** production feed (needs prod `FEED_SECRET` set → else the endpoint is `503 feed_disabled`) + confirm the shared-link OG card resolves; (5.3) manual browser check of the stale-build countdown with JS on/off. **Applied + archived this session (arrived pre-proposed via explore→propose).** _Out of scope: v2 training-derived `progress` number, editorial CMS, custom domain._
- **`add-threshold-history` — ARCHIVED 2026-07-14** (synced 4 ADDED `athlete-config` reqs + 1 ADDED `mcp-server` req; commits `95cfbfa` feat + `11c3c2e` archive). The `athlete_config` singleton only knew *current* physiology (every PUT full-replaces); this versions it so threshold progression ("FTP 240→255→270") is answerable from data. **Migration 059**: append-only `athlete_config_history` — `effective_from DATE PK` + the **16 physiology columns** (same types + `>0` CHECKs) + timestamps; **migration-time seed** from the existing singleton at the **`1970-01-01` epoch sentinel** ("oldest known state, assume it for everything earlier") so `ConfigAsOf` is total whenever a config exists (fresh DB seeds nothing → first PUT snapshots). `ThresholdSnapshot` type **embeds `AthleteConfig`** so the 16 fields + the row's own created/updated timestamps inline into JSON. **`Service.Put` gained a pool** (signature ripple: server.go + 5 test files) and now runs singleton-upsert + history-maintenance in **one `pgx.Tx`**: snapshot today's full state **iff** it differs from the prior singleton (pointer-aware `physiologyEqual` over all 16 fields, timestamps excluded — so the **daily Garmin re-PUT of an unchanged config appends nothing**); a same-day change **replaces** today's row (PK on effective_from); a **same-day revert** to the day-before snapshot **deletes** today's row (invariant: no two consecutive rows physiology-identical). `GET /athlete-config/history?from=&to=` ascending, optional inclusive DATE bounds, `numfmt.Round1` on the two pace floats at the boundary, **`200 {"history":[]}` when empty (no 404)**, `400 date_invalid`+`field` / `range_invalid`. `Service.ConfigAsOf(date)` (latest `effective_from <= date`, `(nil,nil)` empty) — a resolution **primitive provided but deliberately UNWIRED**: per-sport-TSS (write-time snapshot), step-compliance (zone resolution), race-pacing (compute-on-read) all keep current-value semantics; rewiring each is a per-consumer follow-up. MCP `athlete_config_history_get` (read, optional from/to) beside `athlete_config_get`; golden regen touched only that tool. **ADDED-only deltas** (zero MODIFIED blocks) so it composed cleanly after per-sport-TSS + race-pacing both MODIFIED the shared consumption-gate — verified the gate language + both sibling scenarios survived the sync. `athlete_config_history` classified **export-included** in dataexport (user-authored, small, not re-derivable). Garmin bridge unchanged (detected FTP/LTHR flow in via the existing daily PUT hook; no-change dedup keeps it silent). Verified: physiologyEqual unit + repo date-logic (list ascending/range, as-of before-first/between/on-date/empty, latest-before strict) + service maintenance (snapshot-on-change, no-op dedup, same-day collapse, same-day-revert delete, ConfigAsOf smoke) + HTTP endpoint (snapshot-on-change, no-op dedup, same-day collapse, empty-200, date_invalid/range_invalid, singleton-contract-regression) + MCP golden/integration; **full Go suite green (57 pkg)** after the dataexport classification (lone `races`/`bodyweight` failures were the documented testcontainers `build pool` boot-contention flake — pass isolated with `-p 1`; leaked containers pruned mid-session since Ryuk is disabled). `task swag`/`vet`/`validate --strict` clean; README MCP row. **Applied + archived this session (arrived pre-proposed).** _Non-goals held: rewiring existing consumers to ConfigAsOf, retroactive history edits, a Garmin auto-ingest path._
- **`add-race-priority` — ARCHIVED 2026-07-14** (synced 1 MODIFIED + 2 ADDED `race-fueling-plan` reqs; commits `4998b74` feat + `e5fdb4f` archive). Optional **A/B/C** TrainingPeaks triage `priority` on the race resource (A = full taper + peak, B = mini-taper, C = train through) — a durable signal the coach agent reasons over, **advisory only** (no coupling to the macrocycle A-race anchor in either write direction). **Migration 058**: nullable `priority TEXT CHECK (priority IN ('A','B','C'))`, no default, no back-fill (the `training_focus` precedent — absent = untriaged). Typed `Priority` enum + `valid()` mirroring `Discipline`; `omitempty` on the wire. Writable on POST and **tri-state on PATCH** (`"A"` sets, `""` clears to null, omitted leaves unchanged — the empty-string sentinel → `ClearPriority` handler conversion, workouts `training_focus` pattern); repo `UpdateRace` grew a `CASE WHEN $bool THEN $val ELSE priority` arm. Invalid values → `400 race_priority_invalid` (validated at create/patch/list). **List filter** `GET /races?priority=A` (the list endpoint's first query param; `ListRaces(ctx, *string)`, nil = all). MCP `create_race`/`update_race` args gain `priority` (update documents the empty-string clear; `*string` + `omitempty` correctly forwards `""` since omitempty checks pointer-nil not string-empty), `list_races` gains the filter; golden regen touched only those three tools. **Caught a prior gap**: the goldengen diff surfaced that `add-race-pacing-plan`'s feat commit had **left the announced-schema golden uncommitted** (the plan/set/clear race-pacing tool schemas were missing from the frozen baseline — the registry-derived integration test passed so it went unnoticed); this change commits the catch-up golden alongside its own changes. Verified: races priority tests (create persist/echo/reject-invalid/omit-key, PATCH tri-state set/clear/omit/invalid-no-change, list filter A/all/invalid, advisory no-coupling) + agenttools registry + MCP golden/integration; **full Go suite green (58 pkg)**; `task swag`/`vet`/`validate --strict` clean. No new table → dataexport unaffected (column-level, drift guard is table-level). **Applied + archived this session (arrived pre-proposed).** _Non-goals held: taper/peak automation, macrocycle-anchor consistency enforcement, priority on the public feed / coach dashboard._
- **`add-race-pacing-plan` — ARCHIVED 2026-07-14** (synced NEW `race-pacing-plan` capability — 8 reqs — + MODIFIED `athlete-config` consumption-gate + ADDED `mcp-server` tools req; commits `ff0a941` feat + `aad7286` archive). The Best-Bike-Split-lite pacing primitive: **compute-on-read** `GET /races/{id}/pacing-plan` deriving per-leg intensity bands from the athlete-config thresholds, the deliberate sibling of the race-fuelling plan (own capability, unit-isolated). New `internal/racepacing/` — multi-repo aggregator (races + athlete-config, own overrides table), the `workoutfueling` pattern. **Migration 057**: `race_leg_pacing_overrides` keyed **`(race_id, ordinal)` NOT `race_legs.id`** so an override survives `PATCH /races/{id}`'s wholesale leg-replace; CHECKs enforce exactly-one-unit-family + `low≤high` + positive. **Pure `compute.go`**: bike power band as %FTP by leg duration (`<45→90–100`, `[45,90)→83–90`, `[90,180)→75–83`, `≥180→68–78`), run pace band ×threshold-pace (`<30→1.00–1.04`…`≥150→1.18–1.28`), swim band ×CSS (`<20→1.00–1.05`…`≥45→1.06–1.12`); IF = bike band-mid / run·swim 1/mult-mid; `estimated_tss = hr×IF²×100` (swim IF³, sTSS); exact spec numbers locked by unit tests (FTP 265×300min → 180–207 W/IF 0.73/TSS 266.5; threshold 270×100min → 297.0–318.6; CSS 105×70min → 111.3–117.6/90.1). **Honest degradation**: an unset threshold → that leg's targets omitted + machine-readable `missing_thresholds` (per-leg + race union), **200 not 422** (per-leg independence; overrides must work threshold-free); transition legs TSS 0, `other`/no-duration legs `source:none`; `tss_complete` false when any swim/bike/run leg is uncomputable or an `other` leg exists. **Overrides win** over the computed band (`source:override`, IF/TSS re-derived from the override midpoint when the threshold is set), a family/discipline mismatch after a leg edit is **ignored at read** (rationale-noted, never applied cross-unit); ordinals with no leg not surfaced. Sentinels 1:1: 404 race_not_found/leg_not_found/override_not_found, 400 override_discipline_mismatch/target_required/unit_conflict/band_invalid; PUT rejects Idempotency-Key centrally. Unit isolation (power `_w`, run `_sec_per_km`, swim `_sec_per_100m`, no shared struct); `numfmt` at boundary (watts int, pace/TSS `Round1`, IF `Round2` — added `Round2Ptr`). MCP `plan_race_pacing` (read) + `set_race_leg_pacing_override`/`clear_race_leg_pacing_override` (**write-confirm**); golden regen. `race_leg_pacing_overrides` classified **export-included** in dataexport (small, user-authored, not re-derivable — contrast the streams exclusion). **Coordination resolved**: the athlete-config MODIFIED block was re-based on the merged main-spec text so per-sport-TSS's consumption language wasn't clobbered (both only widen the consumer list). Verified: ~20 pure-math unit tests (every band boundary + exact scenarios + degradation + override merge/mismatch + totals) + 15 handler integration tests (happy tri, missing-FTP partial, no-config, unknown-race, override round-trip/delete/survives-PATCH, mismatch-at-write + ignore-at-read, leg-not-found, band-invalid, unit-conflict, Idempotency-Key-on-PUT, unit-isolation NotContains) + registry build tests + MCP golden/integration; **full Go suite green (57 pkg)** after the dataexport classification. `task swag`/`vet`/`validate --strict` clean; README MCP table + RUN_LOCAL walkthrough. **Applied + archived this session (arrived pre-proposed).** _Deferred (design non-goals/open Qs): course/GPS modelling, HR-based targets, `race_legs.intensity` nudging the band, folding secondary, a coach-dashboard pacing panel._
- **`add-step-compliance` — ARCHIVED 2026-07-14** (synced NEW `workout-compliance` capability — 5 reqs; commits `370697b` feat + `746b219` archive). **No migration** — a compute-on-read `GET /workouts/{id}/compliance` scoring how a completed workout was executed *inside*, step by step, vs the template it compiled from (adherence = did it happen; compliance = executed as written). New `internal/workoutcompliance/` **leaf package (no repo)** — the route is workout-anchored but the logic needs workouts+templates+training-plan and training-plan already imports workouts, so it can't live in either without a cycle (the `workoutfueling` situation): service + handlers consuming two narrow injected interfaces (`workoutsRepo.GetByIDWithChildren`, `programProvider.EffectiveProgram`), wired after `trainingPlanSvc`. **Reuses `trainingplan.EffectiveProgram` wholesale** (slot `target_overrides` + athlete-config zone→absolute) — never a second resolver; a zone that couldn't resolve arrives still zone-shaped → that step is **unscorable** (`zone_unresolved`), not an error. Expands repeat groups into a flat step sequence with `iteration/of` provenance ("interval 3 of 5"), matches split `i`↔step `i` **iff `len(splits)==len(expanded)`** — on mismatch returns **200 `status:"unavailable"`, `reason:"lap_count_mismatch"`, planned_steps/executed_laps** and no per-step array (a wrong alignment is worse than "can't score"). Per-step: metric by resolved kind (power_w→AvgPowerW, hr_bpm→AvgHR, pace→1000/AvgSpeedMPS, swim_pace→100/AvgSpeedMPS; cadence/rpe/none/actual-missing → unscorable with reason), in_band/under/over with signed `delta` ("20W under" = delta -20) + `deviation_pct`, target score (100 in-band, linear to 0 at 25% out), duration score (±10% band, 0 at ±25%; lap_button/open unscored), step score 0.7·target+0.3·duration; secondary (bike) scored but informational. Overall = **planned-duration-weighted mean** (600s@60 + 60s@100 ≈ 63.6), `steps_scored`/`steps_in_band`, null when nothing scorable; `numfmt.Round1` at the boundary. Structured errors per http-error-shape: 400 `workout_id_invalid`, 404 `not_found`, 409 `workout_not_completed`/`multisport_unsupported`/`no_template_link`/`splits_missing` — **multisport check precedes template check** so a brick doesn't misreport. MCP `workout_compliance` (read tier, one GET, body incl. the unavailable shape verbatim); golden regen (registry-derived, no manual bump). **Compute-on-read, nothing persisted** (verified: a GET leaves `updated_at` unchanged). Verified: ~25 pure unit tests (expansion ordering/provenance/length-property, scoring tables per kind incl. pace/swim-pace derivation + unscorable reasons + tolerance boundaries, weighted overall, null-score, prerequisite-error matrix) + 7 handler integration tests (happy scoring, no-mutation, 12-split repeat expansion, 9-vs-12 unavailable, slot-override band, every error path) + MCP golden/integration; **full Go suite green (57 pkg)**, `task swag`/`vet` clean. **Applied + archived this session (arrived pre-proposed).** _Deferred (open questions): intent weighting, distance-step weight basis, folding secondary into the score, tolerant one-extra-trailing-lap matching, and a dashboard compliance panel._
- **`persist-activity-streams` — ARCHIVED 2026-07-14** (synced NEW `activity-streams` capability — 7 reqs — + MODIFIED `effort-analytics` and `workouts` specs; commits `9b30c19` feat + `774a290` archive). **First change of the day's arc with a migration** (056: `workout_streams` table storing the full contiguous 1 Hz series as native `REAL[]` — 4 B/sample, TOAST-compressed, `UNIQUE(workout_id,stream_type)` so a re-post replaces; plus three nullable `NUMERIC` execution-metric columns on `workouts`, no back-fill). New `internal/activitystreams/`: **owns the stream-ingest entrypoint** — `POST /workouts/{id}/streams` **moved here from effort-analytics** (contract preserved, response gains `streams_stored`), persists power/speed/**heart_rate** arrays (full-replace, delete-all-then-insert so a re-sync with fewer series drops the rest), **delegates the mean-maximal ladder back to effort-analytics** via a new `ComputeAndReplace(ctx, w, power, speed)` (effort-analytics no longer owns ingest or the workouts repo), then derives + stores the metrics. `GET /workouts/{id}/streams` retrieves with optional **bucket-mean downsampling** (`downsample` bounds `[10,5000]` → `downsample_invalid`; `duration_s` always the full-resolution length); `POST /workouts/{id}/streams/recompute` re-derives from stored streams without a re-post. Sentinels `workout_not_found`/`streams_not_found`/`downsample_invalid`. **Math** (design formulas, `≥20-min` gate): NP = ¼-root of the mean 4th-power of the 30 s rolling avg; VI = `Round2(NP/mean power)`; EF = `Round3(NP/mean HR)` else `Round3(mean speed/mean HR)`; aerobic decoupling = `Round1((r1−r2)/r1×100)` over activity halves — **HR zeros excluded from all HR means, <80% valid-HR coverage → NULL** HR-derived metrics (a run without power still gets EF/decoupling; a dropout-heavy HR strap gets neither). `numfmt.Round3` added. Workouts echo `variability_index`/`efficiency_factor`/`decoupling_pct` on read; **POST/bulk cannot write them, PATCH rejects with `field_immutable`** (allowlist). Garmin-bridge `_extract_streams` now also pulls `directHeartRate` (gaps→0, all-non-positive dropped). MCP `recompute_workout_streams` (write tier); **raw streams deliberately NOT exposed over MCP** (chart data, not reasoning inputs). `workout_streams` classified **export-excluded** in `dataexport/inventory.go` (bulky, re-derived on re-sync; the compact derivations are already exported) — this surfaced via the drift guard, not the proposal. Verified: 15 math unit tests (steady/variable-block/run-without-power/dropout-HR/too-short/±decoupling/NP/bucket-mean) + 11 handler integration tests (persist+best-efforts+metrics/replace/legacy-two-series/empty-no-op/downsample+bounds/streams-not-found/recompute+404s/**delete-cascade**) + 4 workouts execution-metric tests + MCP golden/integration + 12 bridge stream tests (153 total pytest); **full Go suite green (54 pkg)** after the dataexport fix (`summary` failure was the documented testcontainers boot-contention flake, passes `-p 1` alone); `task swag`/`vet` clean. **Applied + archived this session (arrived pre-proposed).** _Deferred: per-workout curve overlay from stored streams; historical backfill of pre-056 activities via recompute-after-resync._
- **`add-intensity-distribution` — ARCHIVED 2026-07-14** (synced 3 ADDED `workout-stats` reqs + 1 ADDED `coach-dashboard` req; commits `de62898` feat + `bae0742` archive). Time-in-zone shares — the polarized/pyramidal check — **extending `workout-stats`** (no new capability, no migration, package already wired) over stored `secs_in_zone_*`. `GET /workouts/intensity-distribution?from&to&tz`: window total (5-entry zone 1→5 shares + low/mod/high **band collapse** + **classification** `polarized|pyramidal|threshold|mixed|null` from fixed 20.0/75.0 constants), `by_sport` breakdown, **Monday-start weekly** trend (buckets only for weeks with a completed workout, edge-clipped, no zero-fill), a `by_training_focus` session-count axis + `unclassified_focus_count`. Completed-only, planned excluded, 400-day cap, workout-stats error vocabulary, read-only. **Missing-zone honesty** (PMC pattern): all-null-zones completed workouts excluded from shares but counted per-week + window (`missing_zone_data_count`) so strength/pool sessions don't bias the split; `share_pct` omitted at zero zone time; full precision, `Round1` at boundary. Pure `classify`/`bands` unit-tested (every branch + 20.0/75.0 boundaries + zero→null; deterministic partition, bands always returned so the label is auditable). MCP `intensity_distribution` (read tier, points volume Qs at `training_totals`); golden regen. `/stats` intensity panel: stacked zone-share bar + classification badge + band shares + weekly bars + missing note + empty-state; `useIntensityDistribution` hook. Verified: classification units + endpoint integration (shape/planned-excluded/missing/classification-empty/focus-axis/tz-bucketing/all-400s/unit-isolation) + registry/MCP golden + **60 web tests**; `tsc`, `swag`/`vet`/`build` clean. `apps/web/dist` gitignored, not committed. **Applied + archived this session (arrived pre-proposed).** _Deferred: per-sport/per-week labels, plan-week bucketing, power-zone axis._
- **`add-performance-management` — ARCHIVED 2026-07-14** (synced NEW `performance-management` capability — 5 reqs — + 1 ADDED `coach-dashboard` req + 1 ADDED `mcp-server` req; commits `4e0a7ce` feat + `bdd86a1` archive). The TrainingPeaks PMC: **compute-on-read** CTL/ATL/TSB over completed-workout TSS, no tables/migration (energy-availability posture). New `internal/pmc/`: read-only repo (earliest completed date + per-local-day `SUM(tss)`/NULL-count, start-day tz bucketing) + a **pure EWMA service** (Coggan 42/7-day; `tsb = yesterday's ctl−atl`) warmed from the earliest workout so values are **window-independent** (`seed_date` reported); per-day `ramp_rate = ctl−ctl₋₇` + Monday-start weekly `ramp_alerts` over the **8/week** ceiling; missing-TSS surfaced per-day (`missing_tss_count`) + window-wide (`missing_tss_workouts`), never imputed; `Round1` at the boundary, full precision internally. `GET /performance/pmc?from&to&tz` — completed-only, planned excluded, 400-day cap, workout-stats error vocabulary, **read-only** (no idempotency key); wired next to effort-analytics. MCP `pmc_series` (read tier, one GET), distinguished from the Garmin acute/chronic mirror. Dashboard `/stats` PMC panel: CTL/ATL lines + TSB bars around zero + shaded ramp-alert weeks (visx), 90/180/365 selector, fitness/fatigue/form readout, empty-state; `usePMC` hook + `PMCSeries` types. Verified: EWMA unit fixtures + endpoint integration (shape/planned-excluded/missing/tz/empty/all-400s/idempotency-ignored) + registry/MCP golden + **57 web tests**; `tsc`, `task swag`/`vet`/`build` clean. `apps/web/dist` gitignored (pipeline-built), not committed. **Applied + archived this session (arrived pre-proposed).** _Deferred (open questions): configurable ramp threshold, `initial_ctl` seed override, per-sport PMC split._
- **`add-per-sport-tss` — ARCHIVED 2026-07-14** (synced 3 ADDED `workouts` reqs + 1 MODIFIED `athlete-config` consumption-gate req; commits `f737a77` feat + `42e3a2b` archive). Made the workouts TSS series honest across sports (the PMC prerequisite). Migration **055**: nullable `tss_source` (`garmin|manual|power|pace|hr`) with an enum CHECK + a `(tss IS NULL) = (tss_source IS NULL)` pairing CHECK, back-filling existing `tss` rows by source. Ingest derivation in `buildWorkout` (after IF): precedence **explicit > power (IF²) > pace (rTSS run IF² / sTSS swim IF³ from threshold pace/CSS) > hr (avg_hr/LTHR, any sport) > none**; completed-only; caller `tss` wins (garmin|manual); an `IF > 2.5` skips; fails open when athlete-config is unwired/unset; stored 2dp, `Round1` at the boundary. PATCH: a value → `tss_source='manual'`, `null` clears both, never derives; re-sync full-replace re-applies precedence. `POST /workouts/recompute-tss` re-derives NULL/computed rows against current thresholds (measured rows immutable) → `{examined, updated, by_source}`; mirrored as the `recompute_workout_tss` MCP tool. `tss_source` is response-only (omitempty), never bound from a request; **athlete-config's "capture-only" gate widened** (thresholds now consumed). Verified: deriveTSS unit formulas/gates + migration back-fill + POST/bulk/re-sync/PATCH/recompute integration + MCP golden; the pairing CHECK forced pairing `tss_source` in existing direct-insert test helpers (`sample`/`adhWorkout`/raceprep `seedWorkout`). `task swag`; **full suite green (54 pkg)**. **Applied + archived this session (arrived pre-proposed).** _Deferred (design open questions): GAP for rTSS, the IF>2.5 cap value, and auto-recompute after `PUT /athlete-config`._
- **`public-race-feed` — ARCHIVED 2026-07-14** (synced NEW `public-race-feed` capability — 2 reqs; commits `3536dc7` feat + `6fd515f` archive). Kazper's **first and only unauthenticated data route**: `GET /public/race-feed`, a curated non-PII projection (`{race:{name,race_date}, days_remaining}`) of the active macrocycle's A-race, for an external Strapi shield to cache + re-serve publicly (`friends → frontend → Strapi → X-Feed-Key → Kazper`) while the personal backend stays private. New read-only `internal/publicfeed/` over macrocycles + races: resolves the A-race as the macrocycle whose `[start_date,end_date]` contains today (tie-break latest `start_date`) → race name/date; `days_remaining` in the user tz, floored at 0; no active macrocycle / null `race_id` / missing race degrades to `{race:null,days_remaining:null}`. Access is a **single shared secret** (`X-Feed-Key == FEED_SECRET`, constant-time `crypto/subtle`) — **NOT a bearer identity, no RBAC** (D2); registered on the root engine OUTSIDE `auth.Middleware` (sibling of `/healthz`); disabled `503 feed_disabled` when unset, `401 feed_unauthorized` on missing/wrong key. `FeedSecret` added to config (redacted). Tests: secret-gate (401/503, no container) + resolution/countdown/graceful-null/overlap-tie-break (testcontainers), asserting no PII leaks; **verified live** (no bearer → reachable, key required, `/api/v1` auth unaffected). swag regen; README + RUN_LOCAL document `FEED_SECRET` + the Strapi topology. Strapi content model + public frontend + a v2 training-derived `progress` number are deferred/out-of-repo. **Applied + archived this session (arrived pre-proposed).**
- **`harden-chat-turn-persistence` — ARCHIVED 2026-07-14** (synced 3 ADDED reqs into the `nutrition-chat` spec; commits `7456dc6` fix + `db955bf` archive). The chat loop discarded `AppendTurns` errors at five sites in `internal/chat/service.go`, silently losing assistant turns, surfacing proposals with no stored anchor, or leaving a dangling `tool_use` the upstream API rejected on every later request. Now **every turn-persist failure is terminal**: emit the existing typed SSE `persistence_error` and end the stream without `done`/`proposal` (a `done` never claims durability the store didn't provide). Sites: runLoop terminal-answer + pause (persist-then-propose) + tool-round; `streamConfirm` execute (writes already dispatched → surface, don't continue); `implicitlyRejectPending` now returns an error so `stream` aborts before appending the new user turn (no more malformed mid-history). Titling stays best-effort but logged (WARN); all persist failures log ERROR with `session_id` + site via a new injectable `*slog.Logger` on `Service`. Tests: fake `SessionStore` gains per-site failure injection; 6 new cases (one per site) + existing happy-path loop/confirm untouched — 37 chat tests green, `vet` clean. No API-shape/swag change (`persistence_error` is an existing SSE code). **Applied + archived this session (arrived pre-proposed).** _Cosmetic debt noted: the capability is still slugged `nutrition-chat` — a stale name from `add-chat-backend` predating `expand-chat-to-coach` + `rebrand-to-kazper` (both 2026-06-14); its Purpose header is still `TBD`. A docs-only rename → `coach-chat` is a candidate backlog item._
- **`harden-server-baseline` — ARCHIVED 2026-07-14** (synced NEW `server-hardening` capability — 4 reqs — + 1 ADDED `cli` req + 1 ADDED `athlete-config` req; commits `47dab38` feat + `9ede3ab` archive). A one-pass sweep of the 2026-07-13 gap analysis's baseline gaps, in six independently-verifiable workstreams: **(1)** migration recovery — `kazper migrate --force <version>` (wraps golang-migrate `Force`; bare `--force` rejected) + `store.Migrate` annotates a dirty state with the version + recovery command so a `MIGRATE_ON_START` boot loop is diagnosable; **(2)** MCP parity — new `athlete_config_update` **write-confirm** tool wrapping `PUT /athlete-config` (full-replace, no idempotency key; golden regenerated additively; the MCP surface dispatches all tiers directly, chat-only pause); **(3)** HTTP hardening (`internal/httpserver/middleware.go`) — per-request deadline (`HTTP_REQUEST_TIMEOUT`=30s → `504 request_timeout`; `/chat`, meal-photo, Garmin-proxy exempt), body cap (`MAX_REQUEST_BODY_BYTES`=1 MiB → `413 body_too_large`; self-capped routes exempt), request-id middleware (honor/generate `X-Request-ID`, echo, log, **forwarded through the chat loopback** via new `internal/reqid`), 5xx-at-Error, `http.Server` `ReadTimeout`+`IdleTimeout` (WriteTimeout left unset for SSE); **(4)** opt-in Prometheus `/metrics` (root, no auth) gated by `METRICS_ENABLED` (default off), RED histograms labeled by route **template** on a private registry (new `prometheus/client_golang` dep); **(5)** test hygiene — `internal/numfmt` unit tests, **fixed the known-red MCP E2E** (`create_workout_template → not_found` was **test-data drift** — `NUTRITION_API_URL` missing the `/api/v1` prefix post-`add-api-versioning`, not a behavioral bug; no spec delta), `task test:race`; **(6)** cleanups — dropped the unused root `--config` flag, guarded `task install` codesign behind a darwin check. Verified: `vet` + `swag` (no drift) + full suite (52 pkg green; lone `goals` failure was testcontainers boot contention, passes `-p 1` alone) + `-race` on touched packages + MCP golden + integration (E2E + announced-tools). **Applied + archived this session (arrived pre-proposed).** Non-goals held: rate limiting, in-app TLS, list-row caps, tracing/OTel; the Helm `metrics.serviceMonitor` toggle + backup-freshness gauge are named deploy-side follow-ups.
- **The data-safety pair — COMPLETE (2/2 shipped & archived, 2026-07-13).** Applied and archived back-to-back this session; the two halves are deliberately separate capabilities (`data-backup` = operational/physical, `data-export` = application/logical).
  - **`add-data-backup` — ARCHIVED 2026-07-13** (synced NEW `data-backup` spec — 5 reqs; commits `7bd6e8e` feat + `604fe02` archive). **No Go code** — pure operational surface. `task db:backup` runs `pg_dump -Fc` inside the compose Postgres container → git-ignored `backups/kazper-<UTC>.dump` (`.partial`→rename on success); `task db:restore FILE=… [FORCE=1]` runs `pg_restore --clean --create --if-exists` against the local maintenance DB, `FILE`-required + confirmation-gated, **local container only**. Helm: opt-in `backup.enabled` (default **false** → chart renders zero new objects) → `CronJob` (`pg_dump -Fc` to a chart-managed PVC, keep-newest-N prune only after a successful dump) + PVC, `DATABASE_URL` from the same Secret the Deployment uses (honors `existingSecret`). New `BACKUP.md` (local flow, cluster-PVC retrieval, manual production restore, app-verified restore drill), pointers from README + RUN_LOCAL. Verified: round-trip drill end-to-end against real local data (`migrate` no-op → `/readyz` → known read), guard paths, `helm template` × 3 scenarios + `helm lint`, `task vet`. No swag.
  - **`add-data-export-import` — ARCHIVED 2026-07-13** (synced NEW `data-export` spec — 8 reqs — + 2 ADDED `cli` reqs; commits `141db99` feat + `9969879` archive). New `internal/dataexport/`: inventory-driven (37 exported / 5 excluded), a **drift guard** that fails export loudly on any unclassified live table. `kazper export` writes one deterministic JSON doc (manifest + `row_to_json` rows, fixed table order, PK-ordered, one compact object per line, byte-identical modulo `exported_at`) in a repeatable-read UTC snapshot; `kazper import` is empty-DB-only + exact-migration-head-match + format-version-gated, single transaction with FK-topological insert order read live from `pg_catalog`, `OVERRIDING SYSTEM VALUE` for GENERATED-ALWAYS identity columns (`chat_messages.seq`/`shopping_items.seq` — caught in the round-trip drill), post-insert count verification, full rollback on any error. Cobra subcommands standalone against `DATABASE_URL` like `migrate` (`--out`/stdout, `-`=stdin). 6 testcontainers tests (round-trip byte-identity, drift, secret exclusion, determinism, full import-guard matrix). Verified end-to-end against real local data (192 rows / 16 tables, byte-identical). No REST/MCP surface, no migration, no swag. **Explore→propose→apply→archive not needed — arrived pre-proposed; applied + archived this session.**
- **`extend-plan-adherence-detail` — ARCHIVED 2026-07-08** (synced TWO ADDED requirements into the `workouts` spec: missed-session list + per-week trend). Grew `GET /workouts/adherence` with two always-present read-side views over the same candidate set — **no migration, no new MCP tool**. `missed_sessions`: compact oldest-first list (`id`/`date`/`sport`/`planned_duration_min`/`planned_tss`) of overdue-unfulfilled sessions, capped at **50** with an explicit `missed_sessions_truncated` flag. `weekly`: per-week trend, **plan-week-aware** — with `plan_id`, buckets align to `plan_weeks.ordinal` (+ phase name, `week_start` derived from `training_plans.start_date`); without, Monday-started calendar weeks (empty weeks not zero-filled). `computeAdherence(rows, now, loc)` folds each already-classified row into **both** the window total and a per-week bucket in one pass, so the trend can never disagree with the top line; `AdherenceCandidates` widened its projection (`w.id` always; `ordinal`/phase/plan `start_date` in plan mode via a LEFT JOIN to `training_phases`). `workout_adherence` MCP tool forwards the richer body verbatim (input schema unchanged; announced-schema golden green). Verified: `internal/workouts` green (198s, +6 unit +2 endpoint tests), `task vet`, `task swag`. **Explore→propose→apply→archive all in one session.** Commits `59b35d3` (feat) + `fbca870` (archive). **Deferred (open questions in design.md):** cap-value tuning + zero-filling the trend for a continuous chart axis. Pre-existing unrelated red: MCP E2E `create_workout_template → not_found` (reproduced on a stashed clean tree — not caused by this change).
- **`garmin-bridge-call-resilience` — ARCHIVED 2026-07-08** (commits `ab363d0` feat + `6114e08` archive). The backend proxied every Garmin op to the garmin-bridge behind a shared 30s timeout — a resilience gap when the bridge is slow/unreachable. Shipped: gateway-timeout-resilient bridge proxy calls + async backfill. _(Superseded the stale "uncommitted artifacts in Backlog" note from the 2026-07-06 refresh — it was applied and archived, not left uncommitted.)_
- **The Strava-stats trilogy — COMPLETE (3/3 shipped & archived, 2026-07-05→06).** explore→propose→apply.
  - **`strava-stats-frontend-phase-3` — ARCHIVED 2026-07-06** (synced NEW `effort-analytics` spec — 3 reqs: best-efforts-from-streams, windowed curve, MCP tool — plus coach-dashboard 7→8 and garmin-bridge 19→20 additions; migration head now `053`). New `internal/effortanalytics/`: `POST /workouts/{id}/streams` computes the mean-maximal ladder (5s…60m) from 1 Hz power/speed and stores compact best-effort rows (raw streams discarded — the curve IS that aggregation); `GET /workouts/power-curve` returns the windowed per-duration MAX with contributing workout/date (sport→metric: bike=power, run/swim=speed). `power_curve` MCP tool. Garmin-bridge: `get_activity_details` fetch (guarded) → defensive `directPower`/`directSpeed` column extraction → post-after-bulk join by result index → `/workouts/{id}/streams` (skip on missing/flat/failure, idempotent re-post). Frontend: `/stats` visx log-x curve + bike/run/swim selector (stays visible on empty). **Deferred:** per-workout curve overlay (4.4 — no per-activity data without raw-stream retention) + historical backfill (5.2 — operational). **Caveat:** stream path covered by unit + fake-backend tests but NOT exercised against live Garmin — the `directPower`/`directSpeed` descriptor keys get confirmed on the first real sync (defensive: unexpected shape → no streams, not a crash). Verified: Go (effortanalytics/store-migration/mcp/httpserver) + 144 bridge + 54 web + webembed + vet. Explore-mode reframe: the backend already mirrors Strava-shaped Garmin data (`personal-records`, `gear`, `achievements`, per-workout `splits`/zones) — surfacing it in the SPA is a frontend problem, not a data problem. Split into three proposals: **Phase 1** (frontend-only — router + surface existing endpoints), **Phase 2** (`workout-stats` capability → `GET /workouts/summary` volume totals + heatmap; +MCP `training_totals`), **Phase 3** (gated — `effort-analytics`: bridge ingests Garmin streams → mean-maximal best-efforts, migration `053`, power/pace curve; +MCP `power_curve`).
  - **`strava-stats-frontend-phase-1` — ARCHIVED 2026-07-05** (coach-dashboard spec synced: 2 → 6 requirements — the v1 training-view requirement scoped to the home route, plus ADDED multi-route-SPA / records / gear / workout-detail requirements). Added `react-router-dom`; `App.tsx` → `BrowserRouter` shell over a `Layout` (nav: Dashboard·Records·Gear) + `DashboardView` at `/`. New routes: `/records` (PR best-efforts table + achievements strip), `/gear` (km mileage + muted wear bar, retired dimmed), `/workouts/:id` (summary `Stat` grid + `ZoneTimeStrip` time-in-zone + `SplitsTable`, 404→not-found, no-splits degrades). `WorkoutList` rows now `<Link>` to detail. **Analyst aesthetic only** — reused `Panel`/`Stat`/exported `ZONE_COLORS`, no celebratory treatment. **No backend touch** — reads existing `/personal-records`, `/gear`, `/achievements`, `/workouts/:id`. **Decision resolved in apply:** PR `activity_id` is a Garmin external id (not a Kazper workout id) → PR rows are display-only, no link. Verified: `tsc`+`vite build` clean, **49 web tests** (incl. deep-link + nav routing test), `go test -tags webembed ./internal/httpserver/...` green. `dist` is pipeline-built (gitignored), not committed. **Merged to `main` (ff) 2026-07-05.**
  - **`strava-stats-frontend-phase-2` — ARCHIVED 2026-07-06** (synced a NEW `workout-stats` capability spec — 2 requirements: the summary endpoint + the MCP tool — plus a coach-dashboard addition, 6→7 reqs). New `internal/workoutstats/` package — read-side aggregation over the workouts table, no schema. `GET /workouts/summary?from=&to=&tz=` returns a per-day series (zero-filled every calendar day) + window total, each with count/duration/distance/elevation/kcal + by-sport count. **Present-only summation** of nullable metrics (a workout missing distance doesn't zero the day); **400-day cap** (vs nutrition summary's 92) so YTD is first-class; multisport counts as its own `by_sport` entry; duration is elapsed (no moving-time field). Reuses `workouts.Repo.List(status=completed)` via a small `WorkoutsRepo` interface. `training_totals` MCP tool (`registry_workoutstats.go`) — **note: the MCP surface is registry-derived + golden-gated, there's no hardcoded expected-tools list to bump; regenerated `testdata/announced_schemas.json` via `-tags=goldengen` (+1 line, additive).** Frontend: `/stats` route (Week/Month/YTD toggle → trailing-7 / trailing-30 / Jan-1→today), `StatsTotals` cards, `ActivityHeatmap` (GitHub-style visx SVG grid colored by daily duration), Stats nav link. Verified: workoutstats integration tests (real Postgres), **52 web tests**, webembed serving, mcp golden + integration, `task vet` — all green. `task swag` regenerated `docs/`.
- **The web-dashboard arc is COMPLETE — 3/3 shipped + archived (2026-06-24).** A new browser surface, served same-origin from the binary:
  - `add-api-versioning` (archived 2026-06-24) — **prerequisite cutover.** Relocated every domain endpoint from the root group to `/api/v1` (`config.APIBasePath` is the single source of truth, shared by the Gin group, the in-process chat loopback dispatcher, and the `NUTRITION_API_URL` default); infra (`/healthz`,`/readyz`,`/swagger`) stays at root. Behavior-preserving — same handlers/auth/payloads. All three clients moved in lockstep: MCP client joins base+path (was *replacing* it), mobile pairing QR carries `/api/v1` (**re-pair required**), garmin-bridge `NUTRITION_API_URL` gains the prefix (httpx/Helm). `@BasePath` + `docs/` regenerated. Breaking, all-at-once (no dual-mount). _Caught mid-apply: the chat loopback was a 4th path consumer not in the original tasks._
  - `add-coach-dashboard` (archived 2026-06-24) — **the SPA.** Vite + React + TS + visx + Tailwind training dashboard embedded via `embed.FS`, served at `/`; a new **`web` Basic-auth identity** (`WEB_USER`/`WEB_PASSWORD`, opt-in, full access) gates the shell + its API calls in one realm. Reads the existing `/context/training` + `/context/recovery` + `/fitness-metrics` — **no new data endpoints.** Committed `apps/web/dist` (mirrors the `docs/` precedent) so `go build` needs no Node. Reach it over TLS/Tailscale (Basic is base64, not encryption). _Implemented out-of-band by the owner; spec synced to `auth` + new `coach-dashboard` capability._
  - `expand-coach-dashboard-fitness` (archived 2026-06-24) — **Tier A: render what was already fetched.** The context payloads carried VO₂max, race predictions, training status, scores, watts/kg, FTP, thresholds, full HR+power zones, and recovery extras — all dropped. New panels (Fitness, Race predictions, Power & thresholds, Zones as banded strips), extended Recovery (+sleep score/stress/body battery), Header status badge, by-sport chips. New format helpers (`raceTime`/`pace`/`pace100`) + a `training_status`→badge map; `AthleteConfig` TS type filled to the full Go shape. **Pure frontend, no backend touch.** ZoneStrip is CSS-flex (proportional bands), not visx. _Deferred: Tier B (weight/race/PR boards) and Tier C (fueling/EA/nutrition lens) stay out of scope._
- **`add-companion-push` (archived 2026-06-23) — the app half of Garmin relogin push.** Wires Firebase Messaging into the Android companion so the backend's relogin push (shipped in `add-garmin-relogin-push`) actually reaches the phone and deep-links into the existing Garmin connect sheet — registers the FCM token while paired, re-registers on refresh, deregisters on unpair (registration survives a denied permission). `google-services.json` is an operator-supplied, uncommitted prerequisite. Android-only; no backend change. _Verified live this session: a test FCM send landed on the device._
- **`add-helm-fcm-push-config` (archived 2026-06-24) — deploy-side of push.** The binary already gated push on `FCM_PROJECT_ID` + `FCM_SERVICE_ACCOUNT_JSON`, but the Helm chart didn't expose them; this wires both through values → Secret → pod env (and confirmed the same `envFrom: secretRef` path carries `WEB_USER`/`WEB_PASSWORD` for the dashboard). `deployment-pipeline` spec updated.
- **The workout-target arc is COMPLETE — 5/5 shipped + archived** (surfaced 2026-06-15 from the Garmin step-editor screenshots):
  - `resolve-zone-targets` (archived 2026-06-15) — the engine: `EffectiveProgram` resolves zone-reference targets to absolute `power_w`/`hr_bpm` from the athlete-config singleton (bike-gated power per D7, HR cross-sport, missing-config passthrough, `origin` provenance). **`athlete-config` is no longer capture-only.**
  - `add-swim-pace-targets` (archived 2026-06-15) — distinct `swim_pace` kind, sec/100m, swim-restricted, bridge converts `100/sec_per_100m`.
  - `add-secondary-target` (archived 2026-06-16) — bike-only Primary+Secondary target (different metric family); resolver resolves a zone-kind secondary; bridge emits `secondaryTarget*`.
  - `add-cadence-target` (archived 2026-06-16) — cross-sport `cadence` kind (bike rpm / run spm), Garmin target type id 3 (`cadence.zone`), registered as a metric family for secondary-target.
  - `add-multisport-structured-workouts` (archived 2026-06-16) — **Phase 1**: new `multisport-workouts` capability (per-sport segments + transitions, `multisport_templates` table, migration `045`), garmin-bridge multi-segment compile, compile-and-schedule action.
  - `multisport-phase-2` (archived 2026-06-16) — **plan integration**: a slot references a single-sport OR multisport template (XOR), materialize emits a `sport='multisport'` planned workout, and `EffectiveProgram` returns per-segment programs resolved **by each segment's sport** (the per-segment resolution Phase 1's D7 deferred) — pushed through the same effective-program→bridge path. Migration `046`. Verified live via API + MCP (XOR validation, materialize idempotency, bike `power_zone→power_w`/run `hr_zone→hr_bpm`/swim passthrough).
  - `multisport-phase-3` (archived 2026-06-16) — **read-time polish**: a multisport template's response carries a derived `estimated_duration_sec` (summed segment durations; null when not fully time-bounded), and the `/context/training` recent-load `by_sport` summary decomposes a `multisport` workout into its segment sports (brick credits swim/bike/run) with graceful fallback. No migration, no new tools.
  - The data back-fill of ~45 zone-ref templates remains a separate coaching task that works independently.
- **`derive-intensity-factor-from-ftp` (archived 2026-06-16) — the last athlete-config consumption.** Bike workouts now derive `intensity_factor = normalized_power_w / ftp_watts` when the watch didn't supply one. With this, the previously-deferred IF-from-FTP consumption is **done** — `athlete-config` no longer has an unconsumed zone/FTP field.
- **`reverse-direction-workout-reconciliation` (archived 2026-06-16) — closes the reconcile loop.** Auto-reconciliation was forward-only (at activity ingest); now **materialize** also adopts a matching unlinked completed activity (so an activity imported before its plan no longer orphans into a duplicate), and a **±1-day tolerance** (same-day preferred) covers cross-day-by-one slippage in both directions. No migration, no new tools; `fulfill`/`unfulfill` stay the manual escape hatch for the >1-candidate/>1-day cases.
- **`plan-adherence-analytics` (archived 2026-06-16) — reads back how well the plan was followed.** New `GET /workouts/adherence` window read: completed/missed/upcoming/unplanned counts, `adherence_rate` over due sessions only (null when none due), planned-vs-actual duration & TSS, `by_sport`; optional `plan_id` scoping via a `plan_slots`/`plan_weeks` join. Pure read (no migration), mirrored as the `workout_adherence` MCP tool. Open follow-up (deferred per spec): a `missed_sessions` list so the coach can name them, and a per-week trend series.
- **`add-companion-train-screen` (archived 2026-06-16) — Flutter companion.** Read-only "Train" screen (fuel-the-training lens) in `apps/companion`; modifies the `mobile-companion` spec. Separate Flutter workstream, committed by its owner (`7e9edb0` + archive `618cc55`).
- **`add-garmin-relogin-push` (archived 2026-06-22) — push the moment Garmin relogin is needed.** Builds on the sync-run log: when the bridge clears its expired token (`DELETE /garmin/token`) and the next `/sync` fails, the user got nothing but a passive `is_stale` flag. Adds a `push-notifications` capability (new `internal/push/`, migration `052`) and wires it as a post-commit side-effect of closing a sync run — an `error` close with the Garmin token absent fires an Android relogin push (latched, gated on push config), a `success` close clears the latch. Side-effects never change the close response contract (a push/latch failure still returns the updated run). **Migration head is now `052` on disk.** _(Not mine — committed out-of-band as `bf2edea` feat + `282a7e5` archive.)_
- **`add-garmin-connect-and-sync-status` (archived 2026-06-22) — Garmin connect + sync freshness, across all 3 langs.** Net-new sync-run tracking (migration `051`, `sync_runs`): the garmin-bridge opens a run before each `/sync` and closes it `success`/`error` after (best-effort — never aborts the data write). New `internal/garminsyncstatus/`: `POST`/`PATCH /garmin/sync-runs` (garmin identity only) + `GET /garmin/sync-status` (mobile+agent → latest run, `last_successful_at`, derived `is_stale`); 503-gated when Garmin unconfigured; read-only `garmin_sync_status` MCP tool. Companion app: a "Garmin" settings section drives the **existing** login/MFA proxy (`POST /garmin/login` → `needs_mfa` → `/garmin/login/mfa`) and shows freshness — **no backend login change** and the app never collects credentials (bridge keeps them). **Migration head is now `051` on disk.** Decisions: `is_stale` threshold 26h, orphaned-`running` handled reader-side only (both revisitable). Deviation: the Flutter Garmin calls hit `api.dio` directly (like `settings_sheet`'s `/healthz`) rather than via `Repository`.
- **`widen-coach-recs-to-memory` (archived 2026-06-21) — coach memory, the cross-surface channel.** Widens `coach_recommendations` → `coach_memory` (migration `050`, rename + `kind`/`text`/`expires_at`/`review_at`/`status`, back-fill): a recommendation is now one `kind` alongside dateless `fact`/`preference`/`constraint`/`observation`, with a review/expire lifecycle and a confirm-in-place `PATCH` (lifecycle-only, preserves `created_at`; content edits stay delete + re-log). Package `internal/coachrecs` → `internal/coachmemory`; routes `/coach/recommendations*` → `/coach/memory*`. MCP family `remember`/`list`/`get`/`update`/`delete_coach_memory`; chat `remember` at write-confirm; **explicit writes only**. Folded into `/context/daily` + `/context/training` (active, non-expired; standing kinds always, recommendations window-scoped; `needs_review` flagged) — the principle held: **DB is the shared brain, transcripts stay private to their surface.** **Migration head is now `050` on disk.** Spec mechanics: capability rename — `coach-recommendations` spec folder retired by hand (REMOVED-all delta can't empty a spec). Coverage caveats: legacy-row back-fill runs on an empty test DB (not seeded); MCP one-request leans on golden/announcement tests.
- **`add-macrocycle-planning` (archived 2026-06-21) — the season layer.** A macrocycle (season) sits above training phases (migration `049`): phase↔macrocycle linkage (`macrocycle_id`/`ordinal`, `target_weekly_tss`/`hours`), a `/context/training` season block (race anchor, days-to-race, current-period position), and `create`/`list`/`get`/`update`/`delete_macrocycle` MCP tools. Committed independently (`dccaa68` feat + `d25a968` archive) — note those commits omitted the MCP golden + swagger regen, which `widen-coach-recs-to-memory` (`76dc10c`) then fixed.
- **`add-workout-training-focus` (archived 2026-06-21) — workout intensity classification.** Adds an optional `training_focus` enum on workouts (migration `048`, CHECK-constrained, nullable, no back-fill) classifying each session's intensity band against the 7-zone German Trainingsbereiche model (`recovery`/`basic_endurance_1`/`basic_endurance_2`/`development`/`competition_specific`/`peak`/`strength_endurance`). Settable on POST/bulk, tri-state on PATCH, returned omitempty, validated to `training_focus_invalid`. Declared intent (not derived from `secs_in_zone_*`) and not sport-coupled. `Merge`/`RestorePlanned` left untouched so a planned prescription's focus survives reconciliation. No new MCP tools (rides the existing workout shape). **Migration head is now `048` on disk.** Follow-up considered & deferred: a Garmin re-sync still clobbers `training_focus` to NULL (full-replace UPSERT, same as rpe/gi) — flip to `COALESCE` if that bites.
- **`persist-coach-recommendations` (archived 2026-06-16) — priorities #6F, done.** New `coach-recommendations` capability (`internal/coachrecs/`, migration `047`): a thin dated, scoped log the coach writes as it advises and reads back across sessions — `POST`/`GET`-window+scope/`GET{id}`/`DELETE` under `/coach/recommendations` + four MCP tools. Deliberately a **storage primitive** — stores authored text verbatim, never synthesizes, and recording a recommendation never mutates an enforced goal/override (the #6F "agent synthesizes, API records" line held). No PATCH (corrections = delete + re-log). **Migration head is now `047` on disk.**
- **The chat→coach unification arc is COMPLETE — 4/4 shipped + archived.** `expand-chat-to-coach`, `add-coach-context-endpoints`, `unify-mcp-tool-registry`, and `rebrand-to-kazper` (archived 2026-06-14).
- **The "mirror everything" Garmin arc: COMPLETE — archived** (`add-garmin-{workout-detail,daily-energy,gear-and-prs,athlete-config,misc-mirror,history-backfill,sync-rolling-lookback}` + `garmin-workout-library-mgmt`, plus `extend-recovery-fitness`). Migrations 036–041 landed; multisport Phase 1 added `045_add_multisport_templates` and **`multisport-phase-2` added `046_add_multisport_plan_integration`; `persist-coach-recommendations` then added `047_add_coach_recommendations`, `add-workout-training-focus` added `048`, `add-macrocycle-planning` added `049_add_macrocycles`, `widen-coach-recs-to-memory` added `050_widen_coach_recs_to_memory`, `add-garmin-connect-and-sync-status` added `051_add_sync_runs`, and `add-garmin-relogin-push` added `052`, so head is now `052` on disk.** Re-verify the head before any future `task migrate:new`.
- **Recent garmin-bridge fixes archived (2026-06-15):** `fix-garmin-bridge-{athlete-config-mapping,threshold-pace-unit,training-status-mapping}`, `drop-phantom-swim-threshold-mapping`, `schedule-adhoc-yoga-mobility`, `surface-athlete-readiness-context`.
- **The PRIOR Garmin + Option B training-plan arc is COMPLETE and archived** — auth, read-import, login, workout-templates → training-plan → garmin-scheduling → plan-slot-targets → workout-reconciliation, plus `fix-chat-tool-status-chips`.
- **Drift to clean up (carried):**
  - ~~**`main` is well ahead of `origin/main`**~~ — **RESOLVED 2026-07-14: pushed.** Everything through the `add-public-race-site` feat/archive commits + the docs/tooling syncs is on `origin/main`. Note the push triggers the first `public-site` workflow run, which fails until its four operator follow-ups (Pages + Actions secret/variable, live verify) are done.
  - **Re-pair the companion app** — `add-api-versioning` changed the pairing base URL to include `/api/v1`; the currently-paired phone still points at the old root and must re-scan a fresh QR (`task dev:pair`). Also unresolved: which LAN/Tailscale IP the phone actually reaches the Mac on (the stale pairing pointed at a `172.16.3.x` address this Mac no longer has).
  - **Stale branches to prune:** `feat/add-chat-sessions` (now == `main`) and `feat/add-recommend-workout-fuel` (leftover) — both safe to delete when convenient (this skill never prunes branches).
- **Open follow-ups (not proposed):** manual on-device smoke for `fix-chat-tool-status-chips` + `expand-chat-to-coach` phase 4 (4.6); a real-Anthropic `/chat` smoke once `ANTHROPIC_API_KEY` is set (503 `chat_unavailable` until then); the derived sweat-rate (ml/hr) endpoint (T2 #6C); plan-adherence `missed_sessions` list + per-week trend series (deferred in the `plan-adherence-analytics` spec). _(Plan-adherence analytics, add-companion-train-screen, reverse-direction reconciliation, IF-from-FTP, and multisport Phase 3 all shipped — see Notes.)_
- **Still-open triage work** (formerly tracked in the now-removed `openspec/priorities.md`; recover full framing from git history if needed): retroactive freeform→product correction, supplement log, derived sweat-rate (ml/hr) endpoint. _(coach_recommendation persistence shipped — see Notes.)_
- **Pattern notes (carried):** MODIFIED spec deltas are full-replace — copy prior scenarios into the MODIFIED block; prefer ADDED requirements for additive intent. **A delta authored before a sibling lands will silently drop the sibling's language on a blind replace** — the whole workout-target arc hit this on the shared garmin-bridge requirement, so each archive *merged* cadence/secondary/swim_pace/multisport rather than replacing. OpenSpec requirement bodies must lead with a SHALL/MUST sentence or `validate --strict` rejects them. The `openspec instructions … --json` command prints a progress line before the JSON — strip with `sed -n '/^{/,$p'` before parsing.

---
_To update: ask Claude "update continuity", "queue X next", or "start work on X"._
_Triage framing now lives inline in Up next + Backlog (the standalone `openspec/priorities.md` was retired 2026-07-13)._
_For the historical record of implemented changes, see [`roadmap.md`](roadmap.md)._
