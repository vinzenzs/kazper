# garmin-bridge runtime dependencies.
# Garmin's private API is reached via garminconnect. As of 0.3.x it vendors its
# own auth/token client (curl_cffi + ua-generator, pulled transitively) and no
# longer depends on the now-deprecated garth. When Garmin breaks something the
# fix is bumping garminconnect — that is the whole reason this service is Python
# and not Go (see design.md). Requires Python >=3.12 (garminconnect >=0.3.3).
garminconnect>=0.3.6
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
